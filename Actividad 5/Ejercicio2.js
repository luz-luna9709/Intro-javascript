/* Declarar un arreglo vacío con un ciclo While que se ejecute 5 veces , agrega los elementos al array. */

var arrayvacio=[];
var j=0;

while(j < 5){
 arrayvacio[j] = j;

 console.log("El arreglo es:" + arrayvacio);
  j++;
}